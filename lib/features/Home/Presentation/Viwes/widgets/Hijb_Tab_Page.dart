import 'package:flutter/material.dart';
class HijbTab extends StatelessWidget {
  const HijbTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
