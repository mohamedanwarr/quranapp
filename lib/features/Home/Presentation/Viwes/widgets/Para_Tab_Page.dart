import 'package:flutter/material.dart';
class ParaTap extends StatelessWidget {
  const ParaTap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
