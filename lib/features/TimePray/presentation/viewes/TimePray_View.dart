import 'package:flutter/material.dart';
class TimePray extends StatelessWidget {
  const TimePray({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
